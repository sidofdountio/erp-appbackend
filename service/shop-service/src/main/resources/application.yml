# Application yml in shop service module
spring:
  application:
    name: shop-service
  config:
    import: optional:configserver:http://localhost:8888

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE