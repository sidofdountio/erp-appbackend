# Application yml in auth server service module
spring:
  application:
    name: auth-service
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}

ui:
  app-shop:
    url: ${UI_APP_SHOP_URL:http://localhost:3000}

